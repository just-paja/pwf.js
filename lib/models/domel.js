/**
 * DEPRECATED
 * ----------
 * This object exists only for compatibility reasons. Object called
 * 'domel' was moved into library called pwf-jquery-compat as 'jq.domel'
 * because pwf.js itself has nothing to do with jQuery and must remain
 * free of dependencies.
 *
 * @see https://github.com/just-paja/pwf-jquery-compat
 */
pwf.rc({
	'name':'domel',
	'parents':['jq.domel']
});
